function obj = allPaths(obj)
    % ---------------------------------------------------------------------
    % Returns a table of all paths with a particular direction (horizontal
    % or vertical), the player which created it, and whether each path is
    % valid and/or contains a grammatical error.
    % ---------------------------------------------------------------------
end